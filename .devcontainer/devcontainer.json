{
  "name": "Database Course - PostgreSQL",
  "image": "mcr.microsoft.com/devcontainers/universal:2-ubuntu-22.04",
  "remoteUser": "codespace",
  "postCreateCommand": "sudo apt-get update && sudo apt-get install -y postgresql postgresql-contrib && sudo service postgresql start",
  "customizations": {
    "vscode": {
      "extensions": [
        "ms-vscode.sql-database-editor-preview",
        "cweijan.vscode-postgresql-client2"
      ]
    }
  }
}